import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Allspeak Watch")
            .font(.headline)
            .multilineTextAlignment(.center)
            .padding()
    }
}

#Preview {
    ContentView()
}
