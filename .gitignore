.DS_Store
*.xcodeproj
*.xcworkspace
xcuserdata/
DerivedData/
.swiftpm/
.build/
build/

# Local signing config (CI generates its own with manual signing + pinned profile)
Allspeak/Signing.xcconfig
AllspeakWatch/Signing.xcconfig
AllspeakWatchWidget/Signing.xcconfig

# Local catalog backend config (real read token installed out-of-band)
Allspeak/CatalogConfig.xcconfig

# Release export artifacts
export/
ExportOptions.plist


# Claude project skills are distributed via marketplaces / tuclaw-plugin, not this repo
.claude/
